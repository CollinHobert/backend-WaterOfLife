/*
export function updateReviewById(req, res){
    const id = req.params.id;
    const review = req.body;// Assuming the updated whisky data is sent in the request body
    console.log("PUT REVIEW");
    console.log("id : " + id);

    //Validate the required whisky data
    if (isNaN(review.rating) || !review.rating || !review.comment) {
        return res.status(400).json({ error: 'Missing required data for review' });
    }

    // Execute the insert query with the whisky data
    db.putReviewById(id, review, (error) => {
        if (error) {
            console.error('Error updating review:', error);
            console.log("debug error");
            return res.status(500).json({error: 'Internal server error'});

        }
        // Return a success response
        res.status(200).json({message: 'Review updated successfully'});
    });
}
 */